server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pmo?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.bank.updg: debug
