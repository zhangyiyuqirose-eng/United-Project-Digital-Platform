<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://updg.com/bpmn">

    <!-- F-1003 项目变更审批流程 (Project Change Approval) -->
    <process id="projectChangeApproval" name="UPDG项目变更审批流程" isExecutable="true">

        <startEvent id="startEvent" name="变更申请提交"/>

        <userTask id="pmReview" name="项目经理审核" flowable:assignee="${projectManager}">
            <documentation>项目经理评估变更影响</documentation>
        </userTask>

        <sequenceFlow id="flow1" sourceRef="startEvent" targetRef="pmReview"/>

        <exclusiveGateway id="pmDecision" name="PM审核结果"/>
        <sequenceFlow id="flow2" sourceRef="pmReview" targetRef="pmDecision"/>

        <sequenceFlow id="reject1" sourceRef="pmDecision" targetRef="rejectEnd">
            <conditionExpression xsi:type="tFormalExpression">${pmApproved == false}</conditionExpression>
        </sequenceFlow>

        <userTask id="pmoApproval" name="PMO审批" flowable:assignee="${pmoApprover}">
            <documentation>PMO审批变更申请</documentation>
        </userTask>

        <sequenceFlow id="flow3" sourceRef="pmDecision" targetRef="pmoApproval">
            <conditionExpression xsi:type="tFormalExpression">${pmApproved == true}</conditionExpression>
        </sequenceFlow>

        <exclusiveGateway id="pmoDecision" name="PMO审批结果"/>
        <sequenceFlow id="flow4" sourceRef="pmoApproval" targetRef="pmoDecision"/>

        <sequenceFlow id="reject2" sourceRef="pmoDecision" targetRef="rejectEnd">
            <conditionExpression xsi:type="tFormalExpression">${pmoApproved == false}</conditionExpression>
        </sequenceFlow>

        <!-- 重大变更需UPDG主任审批：预算变更>10% 或 工期变更>30天 -->
        <exclusiveGateway id="majorCheck" name="是否重大变更"/>
        <sequenceFlow id="flow5" sourceRef="pmoDecision" targetRef="majorCheck">
            <conditionExpression xsi:type="tFormalExpression">${pmoApproved == true}</conditionExpression>
        </sequenceFlow>

        <userTask id="directorApproval" name="UPDG主任审批" flowable:assignee="${director}">
            <documentation>重大变更需UPDG主任审批（预算变更>10%或工期>30天）</documentation>
        </userTask>

        <sequenceFlow id="majorChange" sourceRef="majorCheck" targetRef="directorApproval">
            <conditionExpression xsi:type="tFormalExpression">${isMajor == true}</conditionExpression>
        </sequenceFlow>

        <endEvent id="approveEnd" name="变更通过">
            <terminateEventDefinition/>
        </endEvent>

        <sequenceFlow id="minorChange" sourceRef="majorCheck" targetRef="approveEnd">
            <conditionExpression xsi:type="tFormalExpression">${isMajor == false}</conditionExpression>
        </sequenceFlow>

        <sequenceFlow id="flow6" sourceRef="directorApproval" targetRef="approveEnd"/>

        <endEvent id="rejectEnd" name="变更驳回"/>

    </process>
</definitions>
